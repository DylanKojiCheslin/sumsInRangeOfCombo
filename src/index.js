import  {removeTooBig} from "./removeTooBig";

export default function sumsInRangeOfCombo(entries, upperLimit, lowerLimit){
  const entriesWithoutTooBig = removeTooBig(entries, upperLimit);
  //generate powerset without empty set
  //reduce to number of fitting substates
  return false;
}